{{define "base"}}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{{template "title" .}}</title>

    {{template "head" .}}
</head>
<body>
    {{template "content" .}}
</body>
</html>
{{end}}
